package com.yuresko.timer.ui

import androidx.lifecycle.ViewModel
import androidx.lifecycle.viewModelScope
import kotlinx.coroutines.launch

class TimeViewModel : ViewModel() {

    init {
        viewModelScope.launch {

        }
    }
}